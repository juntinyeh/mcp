### For Dev purpose
### Setup this configuration to work with your AmazonQ-cli > .aws/amazonq/mcp.json
### Then go to the local directory (where server.py located) and start Q.
{
  "mcpServers": {
    "aws-wa-sec-review-mcp-server":{
      "command": "python",
      "args": ["server.py"],
      "env": {
        "AWS_REGION": "us-east-1",
        "AWS_PROFILE": "default",
        "FASTMCP_LOG_LEVEL": "DEBUG"
      }
    }
  }
}
